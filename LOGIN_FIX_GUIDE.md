# حل مشكلة 401 Unauthorized في تسجيل الدخول

## المشكلة
عند محاولة تسجيل الدخول، يظهر الخطأ:
```
POST http://localhost:3001/api/auth/login 401 (Unauthorized)
```

## الحلول المطبقة ✅

### 1. تم إصلاح كلمات المرور
- جميع كلمات المرور للدكاترة مشفرة بشكل صحيح
- تم التحقق من صحة كلمات المرور

### 2. بيانات تسجيل الدخول الصحيحة:

**الدكاترة:**
- **Adel Nassim**: `11111111` / `111111`
- **Mohamed Mahfouz**: `22222222` / `222222`
- **Ahmed Amin**: `33333333` / `333333`
- **Magdi Ahmed**: `44444444` / `444444`
- **Rizk Gabriel**: `55555555` / `555555`

## خطوات التحقق:

### 1. تأكد أن السيرفر شغال:
```bash
npm run server:dev
```
يجب أن ترى: `🚀 Smart Campus Assistant API server listening on http://0.0.0.0:3001`

### 2. تأكد من البيانات المدخلة:
- **University ID**: يجب أن يكون 8 أرقام بالضبط (مثل: `11111111`)
- **Password**: يجب أن يكون 6 أحرف على الأقل (مثل: `111111`)

### 3. تحقق من الكونسول:
افتح Developer Tools (F12) وشوف:
- هل الطلب بيوصل للسيرفر؟
- ما هي البيانات اللي بيتم إرسالها؟
- ما هي رسالة الخطأ بالضبط؟

### 4. اختبر تسجيل الدخول من Terminal:
```bash
node scripts/test-login.cjs 11111111 111111
```

إذا نجح من Terminal لكن فشل من الفرونت إند، المشكلة في:
- CORS configuration
- API URL في الفرونت إند
- البيانات المرسلة من الفرونت إند

## حلول إضافية:

### إذا كان الخطأ "Invalid university ID or password":
1. تأكد أن University ID بالضبط 8 أرقام (بدون spaces)
2. تأكد أن Password صحيح
3. جرب تسجيل الدخول من Terminal أولاً للتأكد

### إذا كان الخطأ "Network error":
1. تأكد أن السيرفر شغال على port 3001
2. تحقق من CORS configuration في السيرفر
3. تأكد من `VITE_API_BASE_URL` في `.env` (اختياري)

### إذا كان الخطأ "CORS error":
1. تأكد أن `ALLOWED_ORIGINS` في `.env` يحتوي على `http://localhost:5173`
2. أعد تشغيل السيرفر بعد تعديل `.env`

## للتحقق السريع:

```bash
# 1. تحقق من كلمة المرور
node scripts/check-password.cjs 11111111 111111

# 2. اختبر تسجيل الدخول
node scripts/test-login.cjs 11111111 111111

# 3. إذا فشل، أصلح كلمات المرور
node scripts/fix-passwords.cjs
```

## ملاحظات مهمة:
- ✅ كلمات المرور مشفرة بشكل صحيح
- ✅ الـ API يعمل بشكل صحيح (تم اختباره)
- ✅ البيانات موجودة في قاعدة البيانات

إذا استمرت المشكلة، تحقق من:
1. البيانات المدخلة في صفحة Login
2. رسالة الخطأ الكاملة في Console
3. Network tab في Developer Tools

