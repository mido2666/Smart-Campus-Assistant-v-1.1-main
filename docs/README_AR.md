# 🏫 مساعد الحرم الجامعي الذكي - دليل شامل بالعربية

## 📋 ملخص عام للمشروع

### الهدف

**مساعد الحرم الجامعي الذكي (Smart Campus Assistant)** هو نظام شامل لإدارة الطلاب والكورسات والحضور والغياب في الجامعات، مع مساعد ذكي مدعوم بالذكاء الاصطناعي وخصائص أمنية متقدمة.

### المستخدمون المستهدفون

- **الطلاب**: لتسجيل الحضور، متابعة الكورسات، التواصل مع الأساتذة
- **الأساتذة**: لإدارة الكورسات، إنشاء جلسات الحضور، متابعة الطلاب
- **الإداريون**: لإدارة النظام بالكامل والمستخدمين

### الوظائف الأساسية

1. **إدارة المستخدمين**: تسجيل، دخول، وإدارة الملفات الشخصية
2. **إدارة الكورسات**: إنشاء وتحديث وإدارة الكورسات الأكاديمية
3. **تتبع الحضور الآمن**: نظام متعدد الخطوات للتحقق من الحضور مع حماية من الغش
4. **المساعد الذكي**: مساعد ذكي للأسئلة والدعم للطلاب
5. **الإشعارات الفورية**: تنبيهات أمنية مباشرة وكشف الغش
6. **التحليلات المتقدمة**: تقارير شاملة مع مقاييس الأمان
7. **لوحة الأوامر**: بحث عالمي وتنقل باستخدام اختصارات لوحة المفاتيح

---

## 🏗️ خريطة المعمارية (Architecture Diagram)

راجع ملف [`ARCHITECTURE.md`](./ARCHITECTURE.md) للحصول على مخطط معماري مفصل باستخدام Mermaid.

### نظرة عامة سريعة

```
┌─────────────────────────────────────────────────────────────┐
│                      Frontend (Vite/React)                   │
│                    Port: 5173 (Development)                  │
│  - React 18 + TypeScript                                    │
│  - Tailwind CSS                                            │
│  - React Router                                            │
│  - React Query                                             │
└───────────────────┬─────────────────────────────────────────┘
                    │ HTTP/HTTPS
                    │ REST API + WebSocket
┌───────────────────▼─────────────────────────────────────────┐
│                    Backend (Node.js/Express)                 │
│                    Port: 3001                                │
│  - Express.js                                               │
│  - TypeScript                                               │
│  - Socket.io (WebSocket)                                    │
│  - JWT Authentication                                       │
└───────────────────┬─────────────────────────────────────────┘
                    │ Prisma ORM
┌───────────────────▼─────────────────────────────────────────┐
│              Database (PostgreSQL via Docker)                │
│                    Port: 5433 (Host)                         │
│  - PostgreSQL 15                                            │
│  - Prisma Schema                                            │
│  - Docker Compose                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 التقنيات المستخدمة

### Frontend

- **React 18** - إطار عمل React الحديث مع hooks
- **TypeScript** - تطوير آمن بالنوع
- **Vite** - أداة بناء سريعة وسيرفر تطوير
- **Tailwind CSS** - إطار CSS للمساعدة
- **React Router** - التوجيه على جانب العميل
- **React Query** - إدارة حالة السيرفر

### Backend

- **Node.js** - وقت تشغيل JavaScript
- **Express.js** - إطار عمل الويب
- **TypeScript** - تطوير آمن بالنوع
- **Prisma** - ORM حديث لقاعدة البيانات
- **PostgreSQL** - قاعدة البيانات الأساسية
- **JWT** - رموز المصادقة
- **WebSocket (Socket.io)** - التواصل الفوري

### DevOps & Tools

- **Docker** - الحاوية
- **Docker Compose** - تنسيق الحاويات المتعددة
- **Prisma** - إدارة قاعدة البيانات
- **Jest** - إطار الاختبار
- **ESLint** - فحص الكود

---

## 🚀 البدء السريع

### المتطلبات الأساسية

قبل تشغيل التطبيق، تأكد من تثبيت:

- **Node.js** (v18 أو أحدث)
- **npm** (v8 أو أحدث)
- **Docker** (v20 أو أحدث)
- **Docker Compose** (v2 أو أحدث)

### 1. استنساخ وتركيب

```bash
# استنساخ المستودع
git clone https://github.com/your-username/smart-campus-assistant.git
cd smart-campus-assistant

# تثبيت التبعيات
npm install
```

### 2. إعداد البيئة

أنشئ ملف `.env` في المجلد الجذر:

```bash
npm run env:create
```

أو أنشئ الملف يدوياً:

```env
# Database (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/smart_campus?schema=public"

# JWT Authentication
JWT_SECRET="your-super-secret-jwt-key-min-32-chars-change-in-production"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# Server Configuration
PORT=3001
NODE_ENV="development"

# CORS Configuration
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:5173,http://localhost:5174"

# Bcrypt Configuration
BCRYPT_SALT_ROUNDS="12"

# Optional: OpenAI Integration
OPENAI_API_KEY="your-openai-api-key"
OPENAI_MODEL="deepseek/deepseek-chat"
OPENAI_BASE_URL="https://openrouter.ai/api/v1"

# Optional: Logging
LOG_LEVEL="info"
```

### 3. إعداد قاعدة البيانات

```bash
# بدء قاعدة البيانات (Docker)
npm run db:start

# إنشاء Prisma Client
npm run db:generate

# تطبيق Schema على قاعدة البيانات
npm run db:push

# ملء قاعدة البيانات ببيانات تجريبية
npm run db:seed
```

**بيانات الدخول التجريبية:**

- **طالب**: universityId `12345678`, password `123456`
- **أستاذ**: universityId `11111111`, password `111111`

### 4. بدء السيرفرات

```bash
# Terminal 1: بدء Backend (Port 3001)
npm run server:dev

# Terminal 2: بدء Frontend (Port 5173)
npm run dev
```

### 5. الوصول إلى التطبيق

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3001
- **Health Check**: http://localhost:3001/health
- **Database Studio**: `npx prisma studio` (يفتح على http://localhost:5555)

---

## 📚 الوثائق التفصيلية

### 1. دليل الباك-إند

راجع [`BACKEND_GUIDE_AR.md`](./BACKEND_GUIDE_AR.md) للحصول على شرح مفصل للباك-إند.

### 2. مرجع API

راجع [`API_REFERENCE.md`](./API_REFERENCE.md) للحصول على قائمة كاملة بجميع الـ endpoints.

### 3. مخطط المعمارية

راجع [`ARCHITECTURE.md`](./ARCHITECTURE.md) للحصول على مخطط معماري مفصل.

---

## 🔧 الأوامر المتاحة

### أوامر قاعدة البيانات

```bash
npm run db:start          # بدء قاعدة البيانات (Docker)
npm run db:stop           # إيقاف قاعدة البيانات
npm run db:generate       # إنشاء Prisma Client
npm run db:push           # تطبيق Schema على قاعدة البيانات
npm run db:migrate        # إنشاء وتطبيق migration
npm run db:seed           # ملء قاعدة البيانات ببيانات تجريبية
npm run db:studio         # فتح Prisma Studio
npm run db:clean          # تنظيف ملفات Prisma المؤقتة
npm run db:fix-url        # إصلاح DATABASE_URL تلقائياً
```

### أوامر السيرفر

```bash
npm run server            # بدء السيرفر (tsx)
npm run server:js         # بدء السيرفر (node)
npm run server:dev        # بدء السيرفر مع nodemon
```

### أوامر Frontend

```bash
npm run dev               # بدء سيرفر التطوير
npm run build             # بناء للإنتاج
npm run preview           # معاينة البناء
npm run dev:network       # بدء مع الوصول للشبكة
npm run dev:check         # فحص حالة سيرفر التطوير
npm run dev:fix-firewall  # إصلاح إعدادات Firewall
```

### أوامر البيئة

```bash
npm run env:check         # فحص متغيرات البيئة
npm run env:create        # إنشاء ملف .env
```

### أوامر الاختبار

```bash
npm test                  # تشغيل جميع الاختبارات
npm run test:watch        # تشغيل الاختبارات في وضع المراقبة
npm run test:coverage     # تشغيل الاختبارات مع التغطية
```

---

## 🔐 الأمان

### ميزات الأمان

- **المصادقة متعددة العوامل**: JWT مع تحكم في الوصول بناءً على الأدوار
- **أمان الموقع**: التحقق من GPS، تحديد الحدود الجغرافية، كشف تزوير الموقع
- **أمان الجهاز**: بصمة الجهاز، التحقق من الجهاز، منع مشاركة الجهاز
- **كشف الغش**: كشف الغش القائم على ML مع تحليل فوري
- **أمان الصور**: التحقق من الصور، تقييم الجودة، كشف التلاعب
- **أمان الوقت**: التحقق من الوقت، التحقق من المنطقة الزمنية، كشف التلاعب بالوقت
- **أمان الشبكة**: التحقق من IP، كشف الوكيل، استخبارات التهديدات

---

## 🐛 استكشاف الأخطاء

### مشاكل شائعة وحلولها

#### 1. خطأ EPERM في Prisma

```bash
# تنظيف ملفات Prisma المؤقتة
npm run db:clean

# إعادة إنشاء Prisma Client
npm run db:regenerate
```

#### 2. قاعدة البيانات غير قابلة للوصول

```bash
# التحقق من حالة Docker
docker ps

# إصلاح DATABASE_URL
npm run db:fix-url

# إعادة تشغيل قاعدة البيانات
npm run db:stop
npm run db:start
```

#### 3. المنفذ قيد الاستخدام

```bash
# Windows: البحث عن العملية
netstat -ano | findstr :3001

# إنهاء العملية
taskkill /PID <PID> /F
```

#### 4. مشاكل CORS

- تأكد من أن `ALLOWED_ORIGINS` في `.env` يحتوي على رابط Frontend
- تأكد من أن Backend يستمع على `0.0.0.0` وليس `localhost` فقط

---

## 📱 النشر والوصول من الموبايل

راجع [`DEPLOYMENT_MOBILE.md`](./DEPLOYMENT_MOBILE.md) للحصول على دليل مفصل للنشر والوصول من الموبايل باستخدام ngrok.

---

## 🧪 الاختبار

### تشغيل الاختبارات

```bash
# جميع الاختبارات
npm test

# وضع المراقبة
npm run test:watch

# مع التغطية
npm run test:coverage
```

---

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT - راجع ملف [LICENSE](../LICENSE) للتفاصيل.

---

## 🙏 شكر وتقدير

- **فريق React** - لإطار عمل React الرائع
- **فريق Prisma** - لأداة ORM الممتازة
- **فريق Tailwind CSS** - لإطار CSS المفيد
- **OpenAI** - لقدرات المساعد الذكي
- **المساهمون** - شكراً لجميع المساهمين

---

## 📞 الدعم

للحصول على الدعم والأسئلة:

- **البريد الإلكتروني**: support@smartcampus.edu
- **الوثائق**: راجع روابط الوثائق أعلاه
- **المشاكل**: https://github.com/your-username/smart-campus-assistant/issues

---

**صنع بـ ❤️ بواسطة فريق مساعد الحرم الجامعي الذكي**
