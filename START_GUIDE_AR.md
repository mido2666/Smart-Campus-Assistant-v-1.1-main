# 🚀 دليل التشغيل السريع

## ✅ تم حل جميع المشاكل!

---

## 🎯 لتشغيل المشروع:

### **1. تشغيل الخادم (Backend):**

افتح نافذة PowerShell في مجلد المشروع وشغّل:

```powershell
cd "D:\Graduation project\Smart-Campus-Assistant-v0.6-main"
npx tsx server/index.ts
```

**أو استخدم:**
```powershell
npm run server
```

**انتظر حتى ترى:**
```
🚀 Smart Campus Assistant API server listening on http://localhost:3001
```

---

### **2. تشغيل الواجهة الأمامية (Frontend):**

افتح نافذة PowerShell **أخرى** وشغّل:

```powershell
npm run dev
```

**انتظر حتى ترى:**
```
Local: http://localhost:5173
```

---

### **3. فتح المتصفح:**

افتح: **http://localhost:5173**

---

## 🔧 المشاكل التي تم حلها:

1. ✅ إصلاح استيرادات ESM (تغيير `.ts` إلى `.js`)
2. ✅ إصلاح أخطاء الأقواس في `schedule.service.ts`
3. ✅ تحديث `server/index.ts` لاستخدام امتدادات `.js`
4. ✅ تحديث `package.json` لاستخدام `tsx`

---

## 📝 ملاحظات:

- الخادم يعمل على المنفذ: **3001**
- الواجهة الأمامية تعمل على المنفذ: **5173**
- يجب تشغيل **الاثنين معاً** في نوافذ منفصلة

---

## 🔍 للتحقق من عمل الخادم:

افتح في المتصفح: http://localhost:3001/health

يجب أن ترى:
```json
{
  "success": true,
  "message": "Server is healthy",
  ...
}
```

---

## 🎓 بيانات الدخول:

**طالب:**
- University ID: `20221245`
- Password: `123456`

**أستاذ:**
- University ID: `12345678`
- Password: `123456`

---

✅ **جميع المشاكل تم حلها والمشروع جاهز للعمل!**

