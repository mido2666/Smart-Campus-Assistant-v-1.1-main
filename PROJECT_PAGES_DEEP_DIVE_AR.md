# تحليل صفحات المشروع: دليل تقني "صفحة بصفحة" 📄🔍

هذا الملف يشرح كيف بنينا كل صفحة في الموقع، التقنيات المستخدمة فيها، والأسرار "تحت الغطاء" (Under the hood) التي يمكن التحدث عنها في العرض.

---

## 🔐 1. صفحة تسجيل الدخول (`Login.tsx`)

### 📝 الوظيفة:
بوابة الدخول، تفرق بين الطالب والدكتور تلقائياً.

### 🛠️ التقنيات المستخدمة:
1.  **React Hook Form & Zod:**
    *   **لماذا؟** لإدارة "استمارة الدخول" وتفيدنا في الـ Validation (التحقق).
    *   **مثال:** تمنع المستخدم من كتابة رقم جامعي أقل من 8 أرقام قبل حتى أن يضغط "دخول". هذا يوفر جهد على السيرفر (Client-side Validation).
2.  **Framer Motion:**
    *   يستخدم لعمل حركات دخول العناصر (Fade In) والتبديل السلس بين "تسجيل الدخول" و "إنشاء حساب".
3.  **Dynamic Background:**
    *   الخلفية ليست صورة ثابتة، بل "CSS Gradient" يتحرك (`animate-gradient-xy`) ليعطي شعوراً بالحيوية والحداثة.
4.  **Local Storage:**
    *   نستخدمها لميزة "تذكرني" (Remember Me). نحفظ الرقم الجامعي مشفراً محلياً لتسهيل الدخول القادم.

### 💡 نقطة تميز للشرح:
> "نحن لا نستخدم الـ Dark Mode في هذه الصفحة عمداً لنعطي شعوراً بالوضوح والترحيب (Force Light Mode)، ولكن بمجرد الدخول، يستعيد الموقع تفضيلات المستخدم (Dark/Light) فوراً."

---

## 🏠 2. لوحة تحكم الطالب (`StudentDashboard.tsx`)

### 📝 الوظيفة:
مركز القيادة للطالب. يجمع بيانات من مصادر مختلفة (جدول، حضور، إعلانات).

### 🛠️ التقنيات المستخدمة:
1.  **TanStack Query (React Query):**
    *   **التقنية الأهم هنا.** تقوم بجلب البيانات (Stats, Schedule) بشكل متوازي (Parallel Fetching).
    *   **Caching:** لو انتقل الطالب لصفحة أخرى وعاد، البيانات تظهر **فوراً** دون تحميل (Fresh Data from Cache).
2.  **Logic for Class Status:**
    *   كتبنا خوارزمية صغيرة في الـ Frontend تحسب: هل المحاضرة "Upcoming" أم "Ongoing" أم "Completed" بناءً على ساعة جهاز الطالب ومقارنتها بتوقيت المحاضرة.
3.  **Loading Skeletons:**
    *   بدلاً من ظهور دائرة تحميل مملة (Spinner)، نُظهر "هيكل" الصفحة رمادياً (Skeleton UI) لتقليل التوتر البصري وشعور المستخدم بسرعة الموقع.

### 💡 نقطة تميز للشرح:
> "هذه الصفحة لا تحمل كل البيانات مرة واحدة. هي تبدأ بالأهم (المحاضرة الحالية)، ثم تحمل الباقي في الخلفية (Lazy Loading) لضمان أقصى سرعة."

---

## 📍 3. صفحة تسجيل الحضور (`StudentAttendance.tsx`)

### 📝 الوظيفة:
أعقد صفحة في المشروع! هي المسؤولة عن عملية الحضور الآمنة.

### 🛠️ التقنيات المستخدمة:
1.  **Geolocation API:**
    *   نستدعي `navigator.geolocation` للحصول على إحداثيات الطالب بدقة عالية.
    *   نطبق معادلة رياضية (Haversine Formula) لحساب المسافة بين الطالب والقاعة بالمتر.
2.  **Camera API & jsQR:**
    *   نفتح كاميرا الموبايل داخل المتصفح مباشرة (بدون تطبيق خارجي) باستخدام `navigator.mediaDevices`.
    *   نستخدم مكتبة `jsQR` لتحليل الصورة واستخراج كود الجلسة المشفر.
3.  **Canvas API (Device Fingerprinting):**
    *   نستخدم الـ Canvas لرسم "صورة خفية" واستخراج كود فريد للجهاز (Fingerprint). هذا يمنع الطالب من تسجيل الدخول بحساب زميله من نفس الموبايل.
4.  **State Machine:**
    *   استخدمنا نمط "الخطوات" (Stepper Logic). لا يمكن للطالب فتح الكاميرا (خطوة 3) إلا بعد نجاح فحص الموقع (خطوة 2). هذا يضمن التسلسل الأمني.

### 💡 نقطة تميز للشرح:
> "نحن نستخدم 3 طبقات حماية هنا تعمل معاً في ثوانٍ معدودة: الـ GPS للكشف عن المكان، الـ Fingerprint للكشف عن الجهاز، والـ QR المتغير للكشف عن التواجد الزمني."

---

## 📅 4. الجدول الدراسي (`Schedule.tsx`)

### 📝 الوظيفة:
عرض الجدول وتصفية المواد.

### 🛠️ التقنيات المستخدمة:
1.  **Client-Side Filtering:**
    *   نحن نجلب الجدول "مرة واحدة"، ثم نستخدم `useMemo` لتصفيته (Filter) في المتصفح. هذا يجعل البحث لحظياً (Instant Search) دون انتظار السيرفر.
2.  **ICS Export (Blob API):**
    *   ميزة مخفية: زر "Export". نقوم بإنشاء ملف تقويم (Calendar File) برمجياً في المتصفح باستخدام `Blob`، ليتمكن الطالب من إضافة جدوله لـ Google Calendar بضغطة زر.

---

## 👨‍🏫 5. لوحة تحكم الدكتور (`ProfessorDashboard.tsx`)

### 📝 الوظيفة:
إدارة المحاضرات الحية ومراقبة الحضور.

### 🛠️ التقنيات المستخدمة:
1.  **Real-time Updates (Socket.io):**
    *   هذه الصفحة تتحدث مع السيرفر باستمرار. أي طالب يسجل حضوره، يظهر الرقم هنا **فوراً** دون تحديث الصفحة.
2.  **Recharts / Custom Charts:**
    *   نستعرض نسب الحضور برسوم بيانية تفاعلية تساعد الدكتور على اتخاذ قرارات سريعة.
3.  **Dialogs (Modals):**
    *   لإنشاء جلسة جديدة بسرعة (Quick Create)، نستخدم نوافذ منبثقة (Modals) فوق الصفحة لعدم تشتيت الدكتور ونقله لصفحة أخرى.

---

## 👤 6. الملف الشخصي (`Profile.tsx`)

### 📝 الوظيفة:
تعديل البيانات وعرض الإنجازات.

### 🛠️ التقنيات المستخدمة:
1.  **FileReader API:**
    *   عندما يرفع الطالب صورة شخصية، نعرضها له فوراً (Preview) قبل رفعها للسيرفر، باستخدام الـ FileReader لقراءة الملف محلياً.
2.  **Optimistic UI:**
    *   عندما يغير الطالب إعداداته (مثل إيقاف الإشعارات)، نغير شكل الزر **فوراً** ليعطيه شعوراً بالسرعة، ثم نرسل الطلب للسيرفر في الخلفية.

---

## ✨ تقنيات مشتركة في كل الصفحات

*   **AuthContext:** هو "العمود الفقري" الذي يغلف التطبيق كله، ويمرر بيانات المستخدم (User Object) لكل صفحة دون الحاجة لطلبها في كل مرة.
*   **Toast System:** نظام الإشعارات الصغيرة التي تظهر في الزاوية (مثل: "تم الحضور بنجاح"). بنيناه ليكون مركزياً ويمكن استدعاؤه من أي مكان بـ `useToast()`.
*   **Responsive Design:** كل الصفحات تستخدم `Grid` و `Flexbox` من Tailwind لتتحول بذكاء من شكل "الكمبيوتر" لشكل "الموبايل" دون أي كود إضافي معقد.
